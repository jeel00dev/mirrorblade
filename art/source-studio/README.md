# Source studio renders

Reserved for white-background concept renders (blade, crest, shop objects) if an image-generation tool becomes available.
Keep the originals here; export cleaned transparent versions to `public/assets/runtime/` and register both in `../asset-manifest.json`.

Nothing in this folder is shipped. As of 2026-09-11 all runtime visuals are procedural (see the manifest), so the folder is intentionally empty apart from this note.
